fn do_something_really_cool(something: i32, something_else: f64): f64 {
    foo := 5 - 12.0

    bar := 25.2

    baz := abs(12 + 16 * 2) * (bar + 12) - 25

    bax := something_else + 2500

    do_something_really_cool
}
