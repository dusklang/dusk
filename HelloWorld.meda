fn do_something_really_cool: i32 {
    // Imagine foo requires a very complicated computation
    foo := do {
        foo := 25
        bar := foo + 25
        baz := bar * 25
        baz
    }

    foo + 25
}
