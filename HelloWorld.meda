fn do_something_really_cool {
    // i32
    hello := 2
    // i32
    hello := 2 + 2
    // f64
    hello := 2.5
    // f64 
    hello := 2.5 + 2.5
    // f64
    hello := 2 + 2 + 2.5
    // f64
    hello := 2.5 + 2 + 2
    // f64
    hello := (2 + 2) * (2.7 + 2) * (2.2 + 2) + 2
}
