I32_MAX :: 2147483647

@precondition(a <= I32_MAX - 2)
@postcondition(return_value == a + 2)
fn add(a: i32): i32 {
    a + 2
}

fn main() {
    // Replace that last 4 with anything larger (such as 5), and it will fail
    var := I32_MAX - 4 + 4 + (-4) + 4
}
