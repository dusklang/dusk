fn memset[T](dest: T *mut, src: T, count: usize) {
    // ...
}

fn main() {
    buffer :: malloc(24) as u8 *mut
    shit(*buffer)
    //memset(buffer, 34, 24)
    //memset[u8](buffer, 34, 24)
}

fn shit[T](arg: T) {}